import type { Metadata } from "next";
import { LeadForm } from "@/components/marketing/lead-form";
import { MarketingPageHero } from "@/components/marketing/page-hero";
import { getPublicShop, phoneHref, shopAddress } from "@/lib/marketing";
export const metadata: Metadata = { title: "Contact the Shop", description: "Call, get directions, review hours, or send a question to the repair shop." };
export default async function ContactPage({ searchParams }: { searchParams: Promise<{ sent?: string; error?: string }> }) { const [shop, query] = await Promise.all([getPublicShop(), searchParams]); const address = shopAddress(shop); const map = address ? `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(address)}` : null; return <><MarketingPageHero eyebrow="Contact" title="Let’s talk about your vehicle" description="Call for the quickest conversation, or send a note and the shop can follow up." /><section className="mx-auto grid max-w-6xl gap-8 px-4 py-16 sm:px-6 lg:grid-cols-[340px_1fr]"><aside className="rounded-3xl bg-slate-950 p-7 text-white"><h2 className="text-2xl font-black">{shop.name}</h2><dl className="mt-7 space-y-5 text-sm"><div><dt className="text-slate-400">Phone</dt><dd className="mt-1 font-bold">{shop.phone ? <a href={phoneHref(shop.phone)}>{shop.phone}</a> : "Contact form available"}</dd></div><div><dt className="text-slate-400">Address</dt><dd className="mt-1 font-bold">{address || "Contact us for directions"}</dd></div><div><dt className="text-slate-400">Hours</dt><dd className="mt-1 font-bold">{shop.hours}</dd></div></dl>{map && <a href={map} target="_blank" rel="noreferrer" className="mt-7 block rounded-xl bg-white px-4 py-3 text-center font-black text-slate-950">Open Map</a>}</aside><div><h2 className="mb-5 text-2xl font-black">Send a question</h2><LeadForm source="CONTACT" sent={query.sent === "1"} error={query.error === "1"} /></div></section></>; }
