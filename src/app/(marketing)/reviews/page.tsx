import type { Metadata } from "next";
import { MarketingPageHero } from "@/components/marketing/page-hero";
export const metadata: Metadata = { title: "Customer Reviews", description: "Learn how the shop approaches feedback and customer care." };
export default function ReviewsPage() { const configured = process.env.PLUMWORKS_GOOGLE_REVIEW_URL?.trim(); const reviewUrl = configured?.startsWith("https://") ? configured : "/contact"; return <><MarketingPageHero eyebrow="Reviews" title="Feedback helps local shops grow" description="Verified customer reviews can be added here after the shop approves the source and wording." /><section className="mx-auto max-w-5xl px-4 py-16 sm:px-6"><div className="grid gap-5 md:grid-cols-3">{["Clear communication placeholder","Friendly local service placeholder","Thoughtful diagnosis placeholder"].map((title) => <blockquote key={title} className="rounded-2xl border border-slate-200 bg-white p-6"><div className="text-orange-500">★★★★★</div><p className="mt-4 font-bold">“{title}”</p><footer className="mt-4 text-xs text-slate-500">Verified review to be supplied by this shop</footer></blockquote>)}</div><div className="mt-10 rounded-3xl bg-slate-950 p-8 text-center text-white"><h2 className="text-2xl font-black">Already visited?</h2><p className="mt-3 text-slate-300">Share your experience through the shop’s configured Google review page.</p><a href={reviewUrl} className="mt-6 inline-block rounded-xl bg-orange-500 px-5 py-3 font-black text-white">{configured ? "Review us on Google" : "Ask for the review link"}</a></div></section></>; }
