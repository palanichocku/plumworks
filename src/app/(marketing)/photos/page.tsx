import type { Metadata } from "next";
import { MarketingPageHero } from "@/components/marketing/page-hero";
export const metadata: Metadata = { title: "Shop Photos", description: "A gallery placeholder for approved repair shop, team, and facility photos." };
export default function PhotosPage() { return <><MarketingPageHero eyebrow="Gallery" title="A look around the shop" description="Client-approved facility, team, and service photos can be managed here in a future storage-backed gallery." /><section className="mx-auto grid max-w-6xl gap-4 px-4 py-16 sm:grid-cols-2 sm:px-6 lg:grid-cols-3">{["Front of shop","Customer area","Service bays","Team at work","Diagnostic equipment","Community moment"].map((label, index) => <div key={label} className={`flex min-h-64 items-end rounded-3xl p-6 text-white ${index % 2 ? "bg-slate-800" : "bg-slate-950"}`}><div><p className="text-xs font-black uppercase tracking-widest text-orange-400">Photo placeholder</p><p className="mt-2 text-xl font-black">{label}</p></div></div>)}</section></>; }
