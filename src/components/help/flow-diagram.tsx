export function FlowDiagram({ title, steps }: { title: string; steps: Array<{ title: string; detail: string }> }) {
  return <figure className="rounded-2xl border border-brand-primary/30 bg-gradient-to-br from-brand-subtle to-white p-6"><figcaption className="mb-5 text-sm font-bold uppercase tracking-wider text-brand-primary">{title}</figcaption><div className="grid gap-3 md:grid-cols-[repeat(var(--flow-columns),minmax(0,1fr))]" style={{ "--flow-columns": steps.length } as React.CSSProperties}>{steps.map((step, index) => <div key={`${step.title}-${index}`} className="relative"><div className="h-full rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><span className="flex size-7 items-center justify-center rounded-full bg-brand-primary text-xs font-bold text-white">{index + 1}</span><h3 className="mt-3 font-bold text-slate-950">{step.title}</h3><p className="mt-1 text-xs leading-5 text-slate-600">{step.detail}</p></div>{index < steps.length - 1 && <span aria-hidden="true" className="absolute -bottom-3 left-1/2 z-10 -translate-x-1/2 rounded-full bg-brand-primary px-2 text-sm text-white md:-right-3 md:top-1/2 md:bottom-auto md:left-auto md:translate-x-0 md:-translate-y-1/2">→</span>}</div>)}</div></figure>;
}
