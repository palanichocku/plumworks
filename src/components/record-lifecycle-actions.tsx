import type { ReactNode } from "react";

export function RecordLifecycleActions({ id, archived, canManage, canDelete, archiveAction, restoreAction, deleteAction, children }: { id: string; archived: boolean; canManage: boolean; canDelete: boolean; archiveAction: (data: FormData) => void | Promise<void>; restoreAction: (data: FormData) => void | Promise<void>; deleteAction: (data: FormData) => void | Promise<void>; children?: ReactNode }) {
  return <div className="flex flex-wrap items-center gap-2">{children}{canManage ? <form action={archived ? restoreAction : archiveAction}><input type="hidden" name="id" value={id} /><button className="rounded-lg border border-slate-300 bg-white px-4 py-2.5 text-sm font-semibold text-slate-700">{archived ? "Restore" : "Archive"}</button></form> : null}{canDelete ? <details className="relative"><summary className="cursor-pointer list-none rounded-lg border border-red-200 bg-white px-4 py-2.5 text-sm font-semibold text-red-700">Delete permanently</summary><form action={deleteAction} className="absolute right-0 z-10 mt-2 w-80 rounded-xl border border-red-200 bg-white p-4 shadow-xl"><input type="hidden" name="id" value={id} /><p className="text-sm font-semibold text-slate-900">This action cannot be undone.</p><label className="mt-3 block text-sm text-slate-700">Type DELETE to confirm<input name="confirmation" autoComplete="off" className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2" /></label><button className="mt-3 rounded-lg bg-red-700 px-3 py-2 text-sm font-semibold text-white">Permanently delete</button></form></details> : null}</div>;
}
